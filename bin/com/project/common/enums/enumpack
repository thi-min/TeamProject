package com.project.common.enums;

public enum AdoptState {
    DONE, REJ, ING
}

public enum FundType {
    MONEY, ITEM
}

public enum AnimalSex {
    MALE, FEMALE
}

public enum AnimalState {
    WAIT, ING, DONE
}

public enum AlarmType {
    LAND, VOLUNTEER, ADMIN_MESSAGE
}

public enum CheckState {
    Y, N
}